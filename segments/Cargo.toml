[package]
name = "segments"
version = "0.1.0"
edition = "2024"
description = "Segmentation of continuous mouse position samples into discrete gesture segments"

[dependencies]
thiserror = "2.0"
clap = { version = "4.6", features = ["derive"] }
log = "0.4"
rstar = "0.13"
serde = { version = "1.0", features = ["derive"] }
